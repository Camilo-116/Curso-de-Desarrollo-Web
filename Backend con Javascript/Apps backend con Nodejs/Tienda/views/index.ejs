<!DOCTYPE html>
<html>
	<head>
		<title>Inventario</title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<h1>Inventario</h1>
		<nav><a href="/?ord=id">Id</a>/<a href="/?ord=nombre">Nombre</a>/<a href="/?ord=precio">Precio</a>/<a href="/?ord=cantidad">Cantidad</a></nav>
		<p>Estos son los productos disponibles:</p>
		<table border>
			<thead>
				<tr>
					<th>Nombre</th>
					<th>Cantidad</th>
					<th>Precio unitario</th>
					<th>Acción</th>
				</tr>
				<% for( let i = 0; i < productos.length; i++ ) { %>
				<tr>
					<td><%= productos[i].nombre %></td>
					<td><%= productos[i].cantidad %></td>
					<td><%= productos[i].precio %></td>
					<td style="text-align: center;"><a href="/eliminar-producto/<%=productos[i].id%>" >X</a></td>
				</tr>
				<% } %>
			</thead>
		</table>
		<p>En total se cuenta con <%= productos.length %> productos.</p>
		<hr />
		<form action="/agregar-producto" method="GET">
			<button type="submit">
				Agregar nuevo producto
			</button>
		</form>
	</body>
</html>
